# Core dependencies for m2m package
# These are installed via pip after conda environment is set up

# Scientific computing (version constraints for AllenSDK compatibility)
numpy>=1.23,<1.24
pandas>=1.5.3,<2.0
scipy>=1.9,<1.11

# Imaging and neuroimaging
nibabel>=3.2
pynrrd>=1.0
h5py>=3.7
tables>=3.7
simpleitk>=2.1
scikit-image>=0.19,<0.20

# Allen Institute SDK
# Note: AllenSDK has strict numpy<1.24 requirement
allensdk>=2.16.0

# ANTs Python bindings
# Note: antspyx may require platform-specific installation
# On some platforms, you may need to install separately with conda
antspyx>=0.5.0; platform_system != "Windows"

# Data handling
xarray>=2022.3.0,<2023.2.0
jinja2>=3.0

# Visualization
matplotlib>=3.5
seaborn>=0.12

# Web application
streamlit>=1.20

# Utilities
requests>=2.28
tqdm>=4.64
